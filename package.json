{
  "name": "web-component-vite",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "build:local": "npm run build && cp env/local/config.json dist/config.json",
    "start": "vite preview"
  },
  "devDependencies": {
    "typescript": "5.9.3",
    "vite": "7.3.1"
  }
}
